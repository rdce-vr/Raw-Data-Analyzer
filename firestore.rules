rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /periods/{periodId} {
      allow read, write: if true;
    }
    match /period_chunks/{chunkId} {
      allow read, write: if true;
    }
  }
}
